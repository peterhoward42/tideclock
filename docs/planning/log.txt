
fart-----


*  main display requirements
*  main display design
*  generative code strategy
*  analysis of api data avail

*  make and deploy the proxy server

o  new data fetch paradigm
	o  rip out stuff don't want 
		*   misleading models and logic - can ignore tests
		*   tell cursor to repair the wreckage
		*  digression classes vs interfaces
		*  introduce cursor to the new design metaphor and ask it to start with
			defining a tide extreme class with simple exposed fields and a
			constructor that demands non nil params for the field data
		*  now ask it to provide a fetcher that massages the returned data into 
		   a new Model called TideEventsForLocation
		*  define application models
		*  testable mapping
		*  helper for which time window is now local
		*  thence to which window are we on utc wise
		o  working with chatgpt on interp of 12 hour slot
			*  put in tide_dial_spec.md into specs dir
			*  have chatgpt modify the overview spec accordingly
				*  first pass from it
				*  me reduce scope
			*  ditch all obsolete docs - they are confusing
		*  put in new 24 hour time services
		*  put in a fetch, transform and store function

		*  now query fn if local data spans the window if so filter it for me
		*  unhappy with undefined query ressponse as sentinel
		*  skill to shorten names
		*  now a fetch via proxy and store followed if new do we have
		*  bake in a town list with lat lon
		*  now create a choose your location UI route top down
		*  have the choice call into the app level change route with logging
		*  update clock to show days events in some way temp via a view model
		*  get started with set location orchestration
		*  sort out view modelling
		*  introduce at least one civic day tide into the model
		o  new display concept
			*  fart
			*  drawexact it
			*  define the geometry in words alone (some scope)
			*  work up iteration workflow process to get right as go along
			*  move on to tick labels
			*  centre cluster
			*  tide marker(s)
			*  stroke widths to clarify preview
			*  time pointer shape
			*  fix tide marker text angle
			*  make tide marker graphic smaller
			*  make tide marker text smaller
			*  fix tide marker text locations
			o  now pointer
				*  get text going
				*  bifurcate for larger theta
				*  dry the branch up 
				*  avoid time now name clashing
				*  fix: actual time inputs used
				*  srp for time now input - beware clash of names
				*  settle on universal encoding of time as  h:m:s int struct
				   time input, with utils generation to derive string
				   representations wherever needed and into float hours in
				   other places
				   	*  no need for str repr of times in inputs - all
					   derivable
					*  cursor is going for it...
				*  Model next event time from tide events not input
				*  Resume tidePointer graphics definition
				*  Next ptr
				*  WaitInterval arc
				*  bring in styling pattern
				*  in-band migration
				*  get real integration working
				*  purge spent docs
				*  get it big enough to see
				*  move time now text out to top right of diagram 
				*  fix: wait time squashed up
				*  some lines dashed
				*  ditch preview iter

				*  split timenow into two with seconds having separate style
				*  purge legacy ref: "is **not** under **CentreCluster**;"
				*  move time now to bottom right
				*  force removal of fallback defaults

				*  make time now animation more eye catching

				o  ditch centre cluster retaining msg standalone and position it
					*  basics
					*  tweak no optional inputs
					o  downgrade the frame to just the arc part

fart

				o  have lost diagram centering
					o  is bottom right timenowlabel smoking gun

				o  say next hight water at foo in bar interval
				o  assemble location and hight water message into one?
				o  put change location inside diag bottom left
				o  try ref arc being a filled shape 
					o  model it with param for thickness and fill style
					o  render tick marks on top of it
					o  move time pointer inside the filled crescent 
					   occluding the tick marks
				o  give now triangle a shimmer effect
				o  tune geom of crescent
				o  change header on home only to dark grey with appropriate changes
				   to menu colour on home route only
				o  improve town picker and redirect to clock
				o  improve visuals incl hierarchy





				o  try on phone
				o  line thicknesses knobs
				o  improve now triangle graphics - perhaps inside tick zone

				o  leave it running to spot errors / null in log
				o  change location ui not great
				o  change location take you to clock route
				o  say suggest use landscape
				o  fit for purpose graphics design styling
				o  not keen on optional code idiom- debate
					o  params
					o  state
					o  loose types such as spec: Record<string, unknown>,
					o  as skill
					o  try it?

				o  network hits observability
				o  how hide browser apparatus at top?
				o  test on tablet and phone
				o  how encourage keep screen on if charging
				o  is it a tide smile?

				o  question merit of keeping iter.sh on gen scene - has
					served its purpose

				o  big comb / housekeep
				o  read the code


				o  Refinements
					o  Say high water in 4 hours, 8 minutes (singular plural)
					o  what if no more tides today?


			o  remove unused junk in generators
			o  inspect the generated code
				o  auto qual checks various


			o  ditch legacy docs
			o  when DRY up the generators for in-app workflow will the
			   .mjs thing still work?


fart



			o  todo
				o  consider baseline angle for now pointer at high
				   TIME values
				o  facilitate user reporting errors
			o  exceptions
				o  no more high waters or low water today
				o  collisions
				o  truncations
				

		o  move on to changing location and firing the refetch, then
		   later can reuse that with the default location
		   	o  start by name -> town -> lat and long

		o  change proxy to fetch earlier start window


Read /docs/planning/new-tidal-data-flow.md for context.

	o  ask cursor to put in getDataForNow as per spec
	

       
o  don't forget
	o  big docco clearout
	o  come back to expiry policy buffer time
	o  prevent locking into system cache miss due to a bug
	o  proxy api should come from envar?
	o  policy for excections
	o  price cap control on worldtimes costs
	o  safety and accuracy disclaimer
	o  about me, gis a job
	o  learn about tides
	o  hire me



