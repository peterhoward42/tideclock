// @ts-check

/**
 * @param {number | null} cachedAtMs
 * @param {number} nowMs
 * @returns {boolean}
 */
export function shouldRefresh(cachedAtMs, nowMs) {
  if (cachedAtMs == null) {
    return true
  }
  void nowMs
  return false
}
