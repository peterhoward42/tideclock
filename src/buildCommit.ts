/**
 * Git commit id baked in at build time (see `vite.config.js`).
 * Temporary: used in About + primary nav to confirm which deploy is loaded; remove when stable.
 */
export const TIDECLOCK_BUILD_COMMIT: string = __TIDECLOCK_BUILD_COMMIT__;
